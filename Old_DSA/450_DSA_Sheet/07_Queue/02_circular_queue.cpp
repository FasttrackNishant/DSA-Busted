#include <iostream>
using namespace std;

class circularQueue
{
public:
    int size;
    int *arr;
    int fr ont;
    int rear;

    circularQueue(int size)
    {
        this->size = size;
        arr = new int[size];
        front = -1;
        rear = -1;
    }

    void push()
    {
        // queue full

        // single element

        // circular queue

        // normal flow

        if (front == 0 && rear == size - 1)
    } 
};
int main()
{
}